<header class="page-header">
  <h1>
  Edit Order:
    <small>
      <%= link_to("ID - #{@order.id}", order_path(@order)) %>
    </small>
  </h1>
</header>

<%= render :partial => 'orders/form', :locals => {:order => @order} %>