<div class="top-section">
  <h2><%= "User #{@user.id} #{@user.first_name} #{@user.last_name} Addresses" %></h2>
  <%= link_to "Create New Address", new_user_user_address_path(@user.id), class: "btn btn-primary btn-lg" %>
</div>

<%= render 'shared/address_table', addresses: @user_addresses %>
