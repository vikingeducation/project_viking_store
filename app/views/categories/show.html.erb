<p class="right"><%= link_to 'Edit', edit_category_path(@category) %> | <%= link_to 'Delete', @category, method: :delete, data: { confirm: "Are you sure you want to delete #{@category}? There is no getting it back. Oh, also it will delete every product associated with it. So there's that. Be sure you're willing to commit to this hugely destructive act." } %></p>
<h1>Category Details</h1>

<h3><%= @category.name %> (#<%= @category.id %>)</h3>
<p><%= @category.description %></p>

<table>
  <thead>
    <tr>
      <th>Products</th>
    </tr>
  </thead>
  <tbody>
    <% @products.each do |product| %>
      <tr>
        <td><%= link_to product.name, product %></td>
      </tr>
    <% end %>
  </tbody>
</table>
