<h2 class="centered">
  Categories
</h2>

<table class="table">
  <% @categories.each do |category| %>
    <tr>
      <td><%= category.name %></td>
      <td><%= link_to "SHOW", "#" %></td>
      <td><%= link_to "EDIT", "#" %></td>
      <td><%= link_to "DELETE", "#" %></td>
    </tr>
  <% end %>
</table>