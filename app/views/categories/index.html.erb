<% @page_title = " | Categories" %>

<% if flash[:notice] %>
	<div class="notice">
		<%= flash[:notice] %>
	</div>
<% end %>

<section>
  <%= render :partial => "./shared/header_button_table",
             :locals => {:table_header => "Viking Store Categories",
 						 :button_label => "Create a New Category",
 						 :button_link => new_category_path,
                         :headers => @column_names,
                         :rows => @categories,
                         :model_name_plural => 'categories'
                         } %>
</section>
