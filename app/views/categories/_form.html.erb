<%=form_for @category do |f|%>

  <% if @category.persisted? %>
    <%= "Category ID: #{@category.id}" %>
  <% else %>
    <p>N/A</p>
  <% end %>

  <%=f.label :name %>
  <%=f.text_field :name %>

  <%=f.label :description %>
  <%=f.text_area :description %>

  <%=f.submit %>

<% end %>