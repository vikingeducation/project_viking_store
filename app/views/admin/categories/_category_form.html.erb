<p>
  Category ID: 
  <%= category.persisted? ? category.id : "N/A" %>
</p>

<%= form_for( [:admin, category] ) do |f| %>
    
    <%= f.label :name %>
    <%= f.text_field :name  %>

    <%= f.label :description %>
    <%= f.text_area :description %>

    <%= f.submit %>

<% end %>