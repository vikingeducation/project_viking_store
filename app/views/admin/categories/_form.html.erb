<%= form_for [:admin, category] do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name, placeholder: "enter category name here" %>
  <%= f.submit "#{(category.persisted? ? 'Update' : 'Create')} Category", class: "btn btn-lg btn-primary btn-block" %>
<% end %>
