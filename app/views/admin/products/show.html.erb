<%= render partial: 'shared/show', locals: { model: @product } %>

<div class="row">
  <div class="col-xs-2">
    <%= link_to "Edit #{@product.class}", edit_admin_product_path(@product.id) %>
  </div>
  <div class="col-xs-2">
    <%= link_to "Delete #{@product.class}", admin_product_path(@product.id), method: "delete" %>
  </div>
</div>
