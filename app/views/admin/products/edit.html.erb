<h1>Edit Product <%= @product.id %></h1>

<%= render partial: "form" %>

<%= link_to "Delete", admin_product_path(@product), method: :delete , class: "btn btn-danger btn-lg btn-block", data: { confirm: "Are you sure you want to delete?" } %>