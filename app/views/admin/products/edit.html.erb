<h1>Edit Product <%= @product.id %></h1>
<%= render partial: 'form', locals: { product: @product,
                                      categories: @categories } %>
<%= link_to "Delete Product", admin_product_path(@product), method: "delete", data: { confirm: "Are you sure you want to delete this product?"} %>
