<div class="row" style="border: 2px solid grey;">
  <div class="col col-xs-12">
    <h1 class="page-header text-center">Edit a User</h1>

    <%= render partial: "form" %>

    <div><%= link_to "View Saved Addresses", admin_addresses_path(user_id: @user.id)%></div>
    <div><%= link_to "Delete this User Account", admin_user_path(@user), method: "delete", data: {confirm: "Are you sure?"} %></div>
  </div>
</div>
