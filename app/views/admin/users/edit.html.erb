<% @page_title = " | Edit User" %>

<% if flash[:alert] %>
  <div class="notice">
    <%= flash[:alert] %>
  </div>
<% end %>

<%= render :partial => "edit_or_new_form",
           :locals => {:crud_header => "Edit User #{@user.id}: #{@user.first_name} #{@user.last_name}",
           						 :form_type => "edit",
                       :object => @user,
                       :submit_label => "Updater User"
                       } %>