<h2>3. Order Statistics:</h2>

<h3>Total:</h3>
<table class="table table-striped table-bordered table-hover">
	<thead>
		<tr>
			<td><b>Item:</b></td>
			<td><b>Data:</b></td>
		</tr>
	</thead>
	
	<!-- Order statistics -->
	<tbody>
		<!-- Highest single order value -->
		<tr>
			<td>Number of Orders:</td>
			<td><%= Order.total_submitted_orders %></td>
		</tr>
		<tr>
			<td>Total Revenue:</td>
			<td><%= number_to_currency(Order.total_revenue) %></td>
		</tr>
		<tr>
			<td>Average Order Value:</td>
			<td><%= number_to_currency(Order.average_order_value) %></td>
		</tr>
		<tr>
			<td>Largest Order Value:</td>
			<td><%= number_to_currency(Order.highest_order_value.order_price) %></td>
		</tr>
	</tbody>
</table>

<h3>Last 7 Days:</h3>
<table class="table table-striped table-bordered table-hover">
	<thead>
		<tr>
			<td><b>Item:</b></td>
			<td><b>Data:</b></td>
		</tr>
	</thead>
	
	<!-- Order statistics -->
	<tbody>
		<tr>
			<td>Number of Orders:</td>
			<td><%= Order.total_submitted_orders{Order.where("checkout_date >= ?",1.week.ago)} %></td>
		</tr>
		<tr>
			<td>Total Revenue:</td>
			<td><%= number_to_currency(Order.total_revenue{Order.where("checkout_date >= ?",1.week.ago)}) %></td>
		</tr>
		<tr>
			<td>Average Order Value:</td>
			<td><%= number_to_currency(Order.average_order_value(1.week.ago)) %></td>
		</tr>
		<tr>
			<td>Largest Order Value:</td>
			<td><%= number_to_currency(Order.highest_order_value.order_price{Order.where("checkout_date >= ?",1.week.ago)}) %></td>
		</tr>
	</tbody>
</table>

<h3>Last 30 Days:</h3>
<table class="table table-striped table-bordered table-hover">
	<thead>
		<tr>
			<td><b>Item:</b></td>
			<td><b>Data:</b></td>
		</tr>
	</thead>
	
	<!-- Order statistics -->
	<tbody>
		<tr>
			<td>Number of Orders:</td>
			<td><%= Order.total_submitted_orders{Order.where("checkout_date >= ?",1.month.ago)} %></td>
		</tr>
		<tr>
			<td>Total Revenue:</td>
			<td><%= number_to_currency(Order.total_revenue{Order.where("checkout_date >= ?",1.month.ago)}) %></td>
		</tr>
		<tr>
			<td>Average Order Value:</td>
			<td><%= number_to_currency(Order.average_order_value(1.month.ago)) %></td>
		</tr>
		<tr>
			<td>Largest Order Value:</td>
			<td><%= number_to_currency(Order.highest_order_value.order_price{Order.where("checkout_date >= ?",1.month.ago)}) %></td>
		</tr>
	</tbody>
</table>