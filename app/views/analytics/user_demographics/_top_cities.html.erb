<table class="table table-striped">
  <tr>
    <th>Item</th>
    <th>Data</th>
  </tr>
  <% User.top_cities.each do |item| %>
  <tr>
    <td><%= item.name %></td>
    <td><%= item.number_of_users %></td>
  </tr>
  <% end %>
</table>