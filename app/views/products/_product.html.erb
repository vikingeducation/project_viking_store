<%= render :partial => 'shared/table', :locals => {:table => {
  :headers => ['Key', 'Value'],
  :rows => product.attributes.keys.map {|a| [a, product.attributes[a]]}
}} %>

<%= render :partial => 'shared/edit_delete_buttons', :locals => {
  :edit_path => edit_product_path(product.id),
  :delete_path => product_path(product.id)
} %>
