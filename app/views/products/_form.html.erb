<%= form_for(product) do |f| %>
  <%= f.label :name %>
  <%= display_error(product, :name) %>
  <%= f.text_field :name, class: 'form-control' %>

  <%= f.submit %>
<% end %> <!-- form_for product -->
