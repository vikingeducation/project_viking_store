<%= form_for @products do |product_field| %>
  <%= product_field.select(:category_id,
                            @product.category,
                            :id,
                            :name) %>
  <%= product_field.submit %>
<% end %>