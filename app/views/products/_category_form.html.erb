<%= form_tag admin_categories_path do %>
  <%= select_tag(:category_id,
                    options_for_select(
                    @category_options,
                    @product.category_id)) %>
  <%= submit_tag %>
<% end %>