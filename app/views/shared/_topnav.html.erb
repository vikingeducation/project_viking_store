<nav class="navbar navbar-default">
  <div class="container">
    <div class="navbar-right">
      
      <%= link_to shopping_cart_path, class: "btn btn-default navbar-btn" do %>
        <span class="glyphicon glyphicon-shopping-cart" aria-hidden="true"></span>
        Shopping Cart 
        <% if cart_size %>
          <span class="badge"><%= cart_size %></span>
        <% end %>
      <% end %>

      <% if signed_in_user? %>
        <%= link_to "Sign Out", signout_path, method: :delete, class: "btn btn-default navbar-btn " %>
      <% else %>
        <%= link_to "Sign In", signin_path, class: "btn btn-primary navbar-btn"  %>
      <% end %>

    </div>
    
    <div class="navbar-header">
      <%= link_to 'Viking Store', root_path, class: "navbar-brand"  %>
    </div>

  </div>
</nav>