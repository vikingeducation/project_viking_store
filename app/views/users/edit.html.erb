<h1>Edit User</h1>

<%= render 'form', user: @user %>

<%= link_to 'Delete', @user, method: :delete, data: { confirm: "Are you sure you want to delete #{@user.name}? There is no getting it back." } %> | <%= link_to 'Cancel', @user %>
