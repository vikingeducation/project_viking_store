<%= render :partial => 'shared/table', :locals => {:table => {
  :headers => ['Key', 'Value'],
  :rows => user.attributes.keys.map {|a| [a, user.attributes[a]]}
}} %>

<%= render :partial => 'shared/edit_delete_buttons', :locals => {
  :edit_path => edit_user_path(user.id),
  :delete_path => user_path(user.id)
} %>
