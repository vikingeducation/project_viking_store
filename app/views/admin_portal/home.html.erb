<%= render "header" %>

<!-- main page row -->
<div class="row justify-content-center mt-4">

  <!-- Left vertical navbar -->
  <%= render "navbar" %>

  <!-- content pane -->
  <%= render "content_pane" %>

</div>
